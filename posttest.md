#### Please attempt the following questions

<br>
Q1. ID3 algorithm selects the attribute with the ________ information gain for splitting at each node.<br>
a. Highest <br>
b. Lowest <br>
c. Medium <br>
d. Average <br>
<br>

Q2. Which algorithm is commonly used to build decision trees?<br>
a. K-nearest neighbors <br>
b. Naive Bayes <br>
c. ID3 <br>
d. Linear regression <br>
<br>

Q3. In ID3, the entropy of a set is calculated using which formula?<br>
a. Entropy(S) = -Σp(x) * log₂(p(x)) <br>
b. Entropy(S) = -Σp(x) * log₁₀(p(x)) <br>
c. Entropy(S) = Σp(x) * log₂(p(x)) <br>
d. Entropy(S) = Σp(x) * log₁₀(p(x)) <br>
<br>

Q4. Which of the following is not a step in building a decision tree?<br>
a. Choosing the root node <br>
b. Selecting features to split on <br>
c. Pruning the tree <br>
d. Calculating probabilities for each class <br>
<br>

Q5. What is the CART algorithm used for in decision trees?<br>
a. Classifying data <br>
b. Regression analysis <br>
c. Both classification and regression <br>
d. None of the above <br>
<br>




